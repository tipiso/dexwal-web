---
import ContactUsSection from '@components/HomePage/ContactUsSection.astro';
import Layout from '../layouts/Layout.astro';
import ProductSection from '@components/HomePage/ProductSection.astro';
import Button from '@components/Button.astro';
const pageTitle = 'About Us';

const keyAspects = [
  {
    number: '1.',
    title: 'Innowacyjne rozwiązania',
    description:
      'Skupiamy się na technologiach strażackich, które zwiększają skuteczność i bezpieczeństwo działań ratowniczo-gaśniczych.',
  },
  {
    number: '2.',
    title: 'Efektywność i bezpieczeństwo',
    description:
      'Nasz sprzęt jest projektowany z myślą o zapewnieniu efektywnych rozwiązań i o maksymalnym bezpieczeństwie użytkowników.',
  },
  {
    number: '3.',
    title: 'Certyfikowane produkty',
    description:
      'Wszystkie produkty zostały wysłane do Centrum Naukowo-Badawczego Ochrony Przeciwpożarowej celem uzyskania opinii technicznej i rekomendacji.',
  },
];
---

<style>
  /* Hero Section */
  .heroContent {
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
  }

  .heroText {
    padding-right: 68px;

    .mainTitle {
      padding-bottom: 32px;
    }
  }

  .building-image {
    width: 100%;
    height: auto;
    border-top-left-radius: 40px;
    object-fit: cover;
  }

  /* Middle Section */
  .middleSection {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }

  .middle-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }

  .middleImg {
    background-color: var(--text-color);
    img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  }
  .middleText {
    margin-top: auto;
    padding: 25px 50px 0;
    background-color: var(--text-color);

    p {
      color: var(--white-color);
      padding-bottom: 25px;
    }
  }
  /* Key Aspects Section */
  .keyAspectsSection {
    padding: 100px;
    background-color: var(--text-color);
  }

  .keyAspectsSection h2 {
    margin-bottom: 56px;
    color: var(--white-color);
    text-align: left;
  }

  .aspects-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 80px;
  }

  .aspectItem {
    text-align: center;
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 0.6s ease,
      transform 0.6s ease;
  }

  .aspectItem.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .aspectNumber {
    font-weight: bold;
    font-size: 90px;
    color: var(--primary-color);
    margin-bottom: 24px;
  }

  .aspectItem h3 {
    color: var(--white-color);
    margin-bottom: 16px;
    text-align: center;
  }

  .aspectItem p {
    line-height: 1.6;
    text-align: center;
    color: var(--white-color);
  }

  /* Animation classes */
  section {
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 0.8s ease,
      transform 0.8s ease;
  }

  section.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Responsive */
  @media (max-width: 992px) {
    .heroContent,
    .middle-content,
    .aspects-grid {
      grid-template-columns: 1fr;
    }

    .heroContent {
      gap: 30px;
    }

    .middleText {
      padding: 40px 20px;
    }

    .aspects-grid {
      gap: 20px;
    }
  }
</style>

<Layout pageTitle={pageTitle}>
  <main>
    <!-- Hero Section -->
    <section class="hero-section">
      <div class="container">
        <div class="heroContent">
          <div class="heroText">
            <h1 class="mainTitle">Dexwal - Nasza historia</h1>
            <p class="bigText">
              <b>Jesteśmy firmą działającą na rynku już od ponad 30 lat.</b> Istniejemy już od 1990 roku. Intensywnie się
              rozwijamy rozprzestrzeniając nasze produkty na skalę europejską.
            </p>
          </div>
          <div>
            <img src="/images/about-company.avif" alt="Siedziba firmy Dexwal" class="building-image" />
          </div>
        </div>
      </div>
    </section>

    <!-- Middle Section -->
    <section class="middleSection">
      <div class="middleImg">
        <img src="/images/about-hose.avif" alt="Pracownik z wyposażeniem" />
      </div>
      <div class="middleText">
        <p class="primaryText">
          Początkowo skoncentrowani na produkcji maszyn rolniczych oraz gumowych i plastikowych komponentów, z czasem
          rozszerzyliśmy naszą działalność o sektor motoryzacyjny, wprowadzając do ofery produkty takie jak maszyny i
          narzędzia rolne. W 2010 roku połączyliśmy się z czeską firmą NL MOTO s. r. o. i rozpoczęliśmy współpracę z NSK
          Bearings.
        </p>
        <p class="primaryText">
          Kolejnym ważnym krokiem w naszej działalności jest rozpoczęcie produkcji węży strażackich. W tym są
          innowacyjnych zwijaczy do węży, platform gaśniczo-chłodzących dla pojazdów elektrycznych oraz dostarczak
          opinia, które są niezbędne do skutecznego prowadzenia działań ratowniczo-gaśniczych. Nasze produkty
          charakteryzuje innowacyjność, ale również najwyższa jakość wykonania i niezawodność. Dzięki czemu nasze
          produkty zdobywają zaufanie strażaków i producentów wozów strażackich.
        </p>
      </div>
    </section>

    <!-- Key Aspects Section -->
    <section class="keyAspectsSection">
      <div class="container">
        <h2 class="mainTitle">Kluczowe aspekty naszej działalności</h2>

        <div class="aspects-grid">
          {
            keyAspects.map((aspect) => (
              <div class="aspectItem">
                <div class="aspectNumber">{aspect.number}</div>
                <h3 class="secondaryTitle">{aspect.title}</h3>
                <p class="primaryText">{aspect.description}</p>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <ContactUsSection />

    <section class="products sectionBreak">
      <ProductSection sectionLabel="Zobacz jakie produkty mamy w ofercie" linkProducts={true} />
    </section>
  </main>
</Layout>

<script>
  // Vanilla JavaScript for animations
  document.addEventListener('DOMContentLoaded', () => {
    // Animate elements when they come into view
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('visible');
          }
        });
      },
      { threshold: 0.1 },
    );

    // Observe all sections
    document.querySelectorAll('section').forEach((section) => {
      observer.observe(section);
    });

    // Observe aspect items for staggered animation
    document.querySelectorAll('.aspectItem').forEach((item, index) => {
      item.style.transitionDelay = `${index * 0.2}s`;
      observer.observe(item);
    });
  });
</script>
